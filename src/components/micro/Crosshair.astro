---
interface Props {
  size?: number;
  class?: string;
}

const { size = 14, class: className } = Astro.props;
---

<span
  class:list={["micro-crosshair", className]}
  data-micro="crosshair"
  style={`--size:${size}px`}
  aria-hidden="true"></span>
